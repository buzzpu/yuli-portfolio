# 圖片規格與尺寸原理

本文件說明本專案 27 個圖片位置的規格要求、尺寸訂定的背後原理、以及匯出與優化流程。

---

## Part 1 · 圖片規格總表

| 位置類型 | 用途 | 比例 | 原始尺寸 | 顯示大小 | JPG 目標 | WebP 目標 |
|---|---|:---:|:---:|:---:|:---:|:---:|
| 專案主圖 | 6 專案 × 2 張 | 4:3 | **1600 × 1200** | ~430 × 322 | ≤ 300 KB | ≤ 180 KB |
| 專案橫幅 | 6 專案 × 1 張 | 16:9 | **2400 × 1350** | ~880 × 495 | ≤ 500 KB | ≤ 300 KB |
| 畫廊主打 (item--1) | 1 張 | 1:1 | **1600 × 1600** | ~528 × 528 | ≤ 400 KB | ≤ 240 KB |
| 畫廊橫幅 (item--2, 9) | 2 張 | 16:9 | **2400 × 1350** | ~1040 × 585 | ≤ 500 KB | ≤ 300 KB |
| 畫廊直式 (item--3, 4) | 2 張 | 3:4 | **1200 × 1600** | ~524 × 699 | ≤ 350 KB | ≤ 210 KB |
| 畫廊方形 (item--5, 8) | 2 張 | 1:1 | **1200 × 1200** | ~524 × 524 | ≤ 300 KB | ≤ 180 KB |
| 畫廊橫式 (item--6, 7) | 2 張 | 4:3 | **1600 × 1200** | ~778 × 584 | ≤ 300 KB | ≤ 180 KB |

**總計 27 張圖，全站頁面總重量目標：≤ 10 MB（JPG）或 ≤ 6 MB（WebP）**

---

## Part 2 · 為什麼是這個尺寸？（尺寸訂定原理）

### 核心公式

    圖片原始尺寸 = 容器顯示大小 × 裝置像素比 × 餘裕係數

三個變數對應三個原則：**設計原則、硬體原則、效能原則**。

---

### 原則一 · CSS 像素 ≠ 裝置像素（Retina 的秘密）

當你在 CSS 寫 `width: 400px`，這是 **CSS 像素**（邏輯像素）。螢幕實際上用多少 **裝置像素** 顯示，取決於裝置像素比（Device Pixel Ratio, DPR）：

| 螢幕類型 | DPR | 400 CSS px = 實體像素 |
|---|:---:|:---:|
| 舊 Windows / 一般外接螢幕 | 1× | 400 |
| **MacBook Retina / iPhone / 高階 Android** | **2×** | **800** |
| iPhone Pro / 頂級手機 | 3× | 1200 |

若只給 400×300 的原始圖，2× 螢幕上會用兩倍像素密度拉伸 → 糊掉。給 2× 原始圖，瀏覽器有足夠像素塞入實體螢幕 → 銳利。

---

### 原則二 · 從 CSS 容器倒推顯示大小

以本站專案 4:3 圖為例：

    外層容器 .project (max-width: 1600px)
      └── .project__grid (grid: 1fr 1.2fr, gap: 100px)
            └── .project__body (佔 1.2/2.2 ≈ 55% = 880px)
                  └── .project__gallery (2 欄, gap: 20px)
                        └── 每張圖 = (880 − 20) ÷ 2 = 430px 寬
                        └── 4:3 比例 → 高 322px

顯示大小：**430 × 322 CSS 像素**

---

### 原則三 · 為什麼是 2×，不是 3× 或 4×

| 倍數 | 原始尺寸 | Retina 顯示 | 檔案大小 | 評價 |
|:---:|:---:|:---:|:---:|---|
| 1× | 430 × 322 | ❌ 模糊 | 30 KB | 太小 |
| **2×** | **860 × 644** | ✅ 銳利 | 120 KB | **甜蜜點** |
| 3× | 1290 × 966 | ✅ 略略更銳利 | 260 KB | 邊際效益低 |
| 4× | 1720 × 1288 | 幾乎看不出 | 450 KB | 浪費頻寬 |

**2× 是甜蜜點的三個理由：**
1. 覆蓋 90%+ 現代螢幕（Retina/HiDPI 主流是 2×）
2. 超越人眼在典型觀看距離下的解析度極限（約 60 PPI）
3. 檔案大小是平方關係成長：3× 的檔案是 2× 的 2.25 倍，收益 vs 成本不划算

---

### 原則四 · 為什麼捨入到 1600×1200 而不是精確的 860×644

三個原因：

1. **標準規格好溝通** — 1600×1200 是攝影、印刷業界通用尺寸，好記、好交付
2. **未來螢幕解析度可能再往上跳** — 留餘裕，未來 3× 普及時仍撐得住
3. **CSS 容器可能會被調整** — 若日後把 max-width 加寬，捨入尺寸能吸收變化

---

### 原則五 · 檔案大小的邊際效益曲線

決定尺寸後，第二個變數是**壓縮品質**。JPG 利用「人眼對某些訊號不敏感」的原理，可以在肉眼看不出差別的前提下大幅縮小：

| JPG 品質 | 1600×1200 檔案大小 | 肉眼可辨？ |
|:---:|:---:|---|
| 100（無損） | ~1.2 MB | — |
| 95 | ~600 KB | 幾乎不可辨 |
| 90 | ~400 KB | 完全不可辨 |
| **82** | **~280 KB** | **完全不可辨** ← 甜蜜點 |
| 75 | ~200 KB | 平滑漸層開始有雜訊 |
| 60 | ~130 KB | 明顯壓縮痕跡 |

**業界共識**：JPG 品質 82、WebP 品質 80 = 肉眼分辨不出的最佳平衡點。

---

### 完整計算範例

以「Streamwalker Tribes 的 Character Sheet」為例：

    Step 1 · 觀察 CSS 結構
      → 每張圖顯示 430 × 322 CSS px

    Step 2 · 套用 Retina 原則
      → 430 × 322 × 2 (DPR) = 860 × 644 實體像素

    Step 3 · 加入餘裕係數，捨入到標準
      → 860 × 644 → 1600 × 1200

    Step 4 · 決定壓縮品質
      → JPG 品質 82 → 檔案 ~280 KB
      → WebP 品質 80 → 檔案 ~170 KB

    Step 5 · 最終規格
      ✅ 1600 × 1200 · WebP · ≤ 240 KB

---

## Part 3 · 檔案格式建議

| 格式 | 何時使用 | 優缺點 |
|---|---|---|
| **JPG** | 照片、遊戲截圖、複雜色彩 | ✅ 通用性 100%<br>⚠️ 檔案略大 |
| **WebP** | 幾乎所有情況（推薦） | ✅ 比 JPG 小 25-35%<br>✅ 支援透明背景<br>⚠️ IE 11 不支援（可忽略） |
| **PNG** | UI 截圖、線稿、需透明背景 | ✅ 無損畫質<br>⚠️ 檔案較大，只用於必要時 |
| **SVG** | 圖示、logo、向量圖 | ✅ 縮放不失真<br>⚠️ 不適用於照片 |
| GIF | 不建議 | 用短影片（MP4/WebM）取代 |

**現代最佳實踐**：**WebP 為主、JPG 為備援**。

---

## Part 4 · 匯出與優化流程

    ① 原始檔（PSD / AI / RAW / 高解析輸出）
            ↓
    ② 依規格表匯出（Photoshop / Figma → 對應比例與尺寸）
            ↓
    ③ 上傳到 squoosh.app 或 tinypng.com 壓縮
       選 WebP 品質 80 或 JPG 品質 80
            ↓
    ④ 命名並放入 images/ 資料夾
       檔名對應 data-slot：streamwalker-01.webp
            ↓
    ⑤ 更新 HTML 的 src 路徑
       src="images/streamwalker-01.webp"

---

## Part 5 · 常見錯誤

**❌ 不要直接上傳手機拍照原圖**  
一張手機照可能 5–10MB，未壓縮直接放上網會嚴重拖慢載入。

**❌ 不要用 4K 或 8K 尺寸**  
除非要放大到滿版全螢幕，否則 2400×1350 是天花板，更大只是浪費頻寬。

**❌ 不要用 PNG 存照片**  
一張 1600×1200 的照片，JPG 300KB，PNG 可能 3MB。差 10 倍。

**✅ 保持一致的畫質**  
同一個專案內三張圖用同樣品質等級匯出，避免有的清晰有的模糊。

**✅ 使用 `loading="lazy"`（HTML 已內建）**  
所有 `<img>` 已加入延遲載入，未進入視窗的圖片不會下載。

**✅ 主體置中**  
CSS 使用 `object-fit: cover` 會等比放大並從中心裁切。重要主體務必置中，否則可能被裁掉。

---

## Part 6 · 比例不吻合會怎樣？

假設你把 3:2 照片放進 4:3 容器——CSS 的 `object-fit: cover` 會做兩件事：

1. **等比放大**直到覆蓋容器
2. **從中心裁切**多出來的邊緣

所以：
- 主體如果在照片邊緣，可能被裁掉
- 若作品不能被裁（例如完整角色卡設定），**請先在 Photoshop / Figma 裡按對應比例裁切好再存檔**

---

## Part 7 · 一句話結論

> **給瀏覽器「顯示大小 × 2」的原始檔，用 JPG 82 / WebP 80 壓縮，就對了。**
> 
> 覆蓋 90%+ 高解析螢幕、檔案控制在人眼不可辨的極限、頻寬用在該用的地方。所有網頁圖片規格的根源，都是這一句話。
